PSUEDO CODE FOR HOP SCOTCH

vector<list> cLists

OBTAIN LOCATION
	if(autoLocation == 1)
		Location = autoLocation
	
	else
		Location = userInputLocation
		
ASK FIRST QUESTION

Questions:
	ASK QUESTION 1+N
		if(A)
			List Q2A = YelpAPI(A, Location)
			cLists.pushback(Q2A)
		else if (B)
			List Q2B = YelpAPI(B, Location)
			cLists.pushback(Q2B)
Main:
	if(Hungry)
		List HungryMaster = YelpAPI(Food, Location)
		cLists.pushback(HungryMaster)
		
		Questions();
